# 작업 로그 — 2026-06-07 (Day 2)

## 목표
장수 시스템 도입 (GDD 7장) + 대량 로스터 + 시나리오별 지역 배치 (GDD 4장).
- 장수 베이스 데이터(정적) / 규칙 함수(love 비의존) / 시나리오 배치 3계층 분리.
- 풀 약 180~200명(실제 후한말~삼국 인물만, CLAUDE 고증 톤).
- 184/194/221 각각 별도 배치. active=세력 소유 지역, free=재야 은신, unrevealed=미배치.
- (로드맵 GDD 20장: 전투/포획/장수관리 단계 진입의 데이터 토대)

## 지시 요약
- 베이스: id/이름/무력·지력·정치·체력/등장연도. 충성·병력·위치·소속·상태·이동·행동완료는 베이스에 두지 말 것.
- 규칙(game_state): 병력 한도=무력 기반(GDD 7장, 계수 상수), 상태 enum 5종, 행동완료 리셋, 조회(지역/세력/재야).
- 배치(시나리오 데이터): officer id → 소속/위치/충성/상태. 30지역 분산, 지역당 평균 약 6명 목표.
- 배치 규칙: active=자기 세력 소유 지역만 / free=중립·임의 지역 은신 / unrevealed=등장연도>연도면 미배치.

## 구현 내용
3계층(CLAUDE 결정 1·2) 그대로 분리해 작업:

| 파일 | 계층 | 변경 |
| --- | --- | --- |
| `config.lua` | 상수 | `config.officer` 추가 — `troopsPerMight=5`(병력 한도 계수), `maxLoyalty=100` |
| `game_data.lua` | 데이터 | `game_data.officers`(베이스 184명) + 시나리오마다 `factions[*].lord` & `officers` 배치 |
| `game_state.lua` | 규칙(신규) | 장수 규칙·런타임 구성·조회·행동 플래그. **love.\* 전무** → 테스트 가능 |
| `tests/test_pure.lua` | 테스트 | 장수 풀·규칙·배치 정합성 검증 35건 추가 |

### 장수 베이스 (game_data.officers, 184명)
- 위/촉/오 + 군웅(동탁·여포·원소·원술·유표·마등·공손찬·정원·하진·유언·유장·장로) + 황건/흑산 + 명사·재야.
- 정적 값만: `id,name,might,intel,pol,hp,appear`. 능력치 0~100(인지도 반영). 충성·병력·위치 등은 제외.
- `appear`(등장연도): "장수로 활동 가능"해지는 대략 연도. 시나리오 연도보다 늦으면 그 시대 unrevealed.
- CLAUDE 고증: 실제 인물만. 초선(연의 가공) 등 가상 인물 미포함.

### 규칙 (game_state.lua)
- `STATE`(active/free/unrevealed/captured/dead) — Lua엔 enum 없어 문자열 상수 테이블로 흉내.
- `troopsCap(o)=might*config.officer.troopsPerMight` (GDD 7장 "무력*5", 계수 상수화).
- `isUnrevealed(base,year)` / `buildOfficers(gameData,scenario)`(베이스+배치→런타임 레코드, 원본 불변).
- 조회: `byId/officersInRegion/officersOfFaction/freeOfficers`.
- 행동: `resetActions`(턴 시작 리셋)/`canAct`(active·미행동·비이동)/`markActed`/`loyaltyText`(군주='-').

### 시나리오 배치 (scenario.officers + faction.lord)
- 각 항목 `{ id, faction?, region, state, loyalty? }`. lord는 충성도 생략('-'); `faction.lord`로 식별.
- 184: 영웅 다수가 아직 무명 → 군주·황건·서량 등 21명 active, 미래 군웅 23명은 재야(free). (조조·유비·관우·장비·원소 등 재야)
- 194: 무명들이 각 세력에 자리잡음 — 11세력 active + 여포 일당·명사 재야.
- 221: 1세대 군웅 퇴장, 위·촉·오 3국 확립. (사망·미등장 인물은 해당 시나리오에서 제외)

## 시도·실패 / 원인 / 수정
- **194 배치에 미래 인물 혼입.** 원담(195)·고간(195)·동소(196)을 194(연도 194)에 active 배치 → 테스트 실패.
  - 원인: appear>year(unrevealed)를 active로 둠. 고증상 194엔 미등장.
  - 수정: 3명 194 배치에서 제거 → 재실행 64/64 통과.
- **standalone Lua 부재(Day1과 동일).** `lua`/`luajit` 미설치.
  - 수정: 임시 `_test_harness/`(main+conf)에서 `package.path`에 src/tests 추가 후 `require("test_pure")`를
    lovec(내장 lua51)로 실행 → 결과 확인 후 임시 dir 삭제.

## 검증
순수 테스트 **64/64 통과** (`lovec _test_harness`, 헤드리스 하네스). 장수 관련 추가 검증:
1. 풀 규모 184명(180~200) / id 중복 0 / 능력치 0~100·필드 유효.
2. 규칙: 병력 한도 공식 / 상태 enum 5종 / isUnrevealed / resetActions·canAct·markActed.
3. 시나리오 배치 정합성(3시나리오 각각):
   - 세력마다 실재 lord 존재 / 배치 id=실재 베이스 / 중복 배치 0.
   - **배치 장수 전원 appear ≤ 시나리오 연도** (unrevealed 미배치).
   - **active 장수 전원 자기 세력 소유 지역(ownership)에 위치.**
   - free 장수 무소속 / 군주 충성도 '-'.

### 지역당 평균 장수 수 (참고 — 정보 출력)
- 184: 44명 / 평균 **1.5명**, 194: 84명 / 평균 **2.8명**, 221: 60명 / 평균 **2.0명**.
- GDD 7장 목표(약 6명)보다 낮음. **의도된 결과**: 풀(184명)은 게임 전체 기준이고, 각 시나리오는
  그 시대에 "등장(appear≤연도)했고 살아있는" 부분집합만 배치하기 때문(미등장·사망 인물 제외).
  6/지역은 풀 설계 상한 지향치로 보고, 평균은 검증 실패가 아닌 정보로 출력. (배치는 데이터라 추후 확장 용이)

## 회고
- 좋았던 점: 데이터(베이스)/규칙(game_state)/배치(시나리오) 경계를 처음부터 분리해
  game_state를 love 없이 35건 자동 검증할 수 있었다. 배치 정합성(active=소유지역, unrevealed 차단)을
  테스트로 강제해 데이터 오류(194 미래 인물)를 즉시 잡았다.
- 아쉬운 점: 사망 연도 필드가 없어 221의 "살아있는 인물" 판정을 수기 큐레이션에 의존(스코프 락 준수).
  지역당 평균이 목표보다 낮음 — 후속 세션에서 시대별 단역을 보강해 밀도를 올릴 여지.
- 다음(GDD 20장): main.lua 우측 패널에 지역 장수 표시(buildOfficers/officersInRegion 연결),
  이후 훈련/징병에 병력 한도·행동완료 플래그 적용.

---

## 추가 작업 — 연도+월 턴 시스템 + 턴 넘기기 + 우측 패널/턴 버튼
1턴 = 1개월(12월 다음 다음 해 1월). GDD 3장 흐름·9장(7월 수확, 매 턴 세금) 따름.

### 지시 요약
- 턴 데이터: 시나리오별 시작 연/월(GDD 4장), 12개월·수확월 상수(매직넘버 금지).
- 턴 규칙(game_state, love 비의존): turn 상태(연/월/카운트), advanceTurn() — GDD 3장 순서 +
  미구현 시스템(도착·성장·AI·수확)은 빈 훅/스텁(호출 순서·시그니처만 확정). 7월 진입 시 수확 훅.
  날짜 진행 후 장수 행동완료 리셋(어제 작업 연결).
- 우측 패널(GDD 17장): 상단 연/월 → 선택 지역 정보 → 최하단 턴 버튼. 버튼=ui.lua, 입력=main, draw 읽기만.

### 구현 내용
| 파일 | 계층 | 변경 |
| --- | --- | --- |
| `config.lua` | 상수 | `config.turn`(monthsPerYear=12, harvestMonth=7) + `config.panel`(폭/여백/줄간격/버튼/배경) |
| `game_data.lua` | 데이터 | 시나리오마다 `startMonth` 추가(184=2 봄거병, 194·221=1) |
| `game_state.lua` | 규칙 | `newTurn(scenario)` + `advanceTurn(turn, ctx)` — 6단계 + 7월 수확 훅 |
| `main.lua` | 표현·입력 | `state.turn/officers`, `panelRect/turnButton/turnContext/advanceGameTurn/drawPanel`, 버튼 클릭·패널 클릭 차단 |
| `ui.lua` | UI | (기존 newButton/hit/draw 재사용 — 턴 버튼) |
| `tests/test_pure.lua` | 테스트 | 턴 검증 15건 추가 |

- **advanceTurn 훅 패턴**: ctx 에 콜백(onTurnEnd/onArrivals/onGrowth/onAI/onHarvest)이 있으면 정해진
  순서로 호출, 없으면 빈 스텁. 시그니처 `function(turn, ctx)` 통일 → 시스템 생기면 콜백만 끼움.
  (Lua 함수 1급값 = C# delegate/Action, C++ std::function 비교 주석.)
- 날짜 진행은 game_state(규칙)만 수행. main 은 버튼 클릭 → advanceGameTurn(=advanceTurn 위임).
- 패널은 화면 오른쪽 고정. 누른 지점이 패널 사각이면 지도 선택 무시(패널이 지도 가림).
- 턴 버튼 클릭은 mousepressed 에서 즉시 판정(드래그 로직 앞) → 누른 즉시 턴 진행.
- 선택 지역 정보는 우측 상단 텍스트 → 패널로 통합(중복 제거). 패널에 지역 장수 수도 표시(어제 작업 연결).

### 시도·실패 / 원인 / 수정
- 큰 문제 없음. UI 배선(버튼·패널) 후 unused-function 경고는 호출부(drawMap/입력) 연결로 해소.
- standalone Lua 부재 → Day1·오전과 동일하게 lovec 임시 하네스로 순수 테스트 실행.

### 검증
1. 순수 테스트 **77/77 통과**(턴 15건 추가): newTurn 연/월/폴백, 월+1, 12월→1월·11→12→1 롤오버,
   7월 진입 시 수확 훅 발화·비수확월·롤오버 시 미발화, 행동완료 리셋, 훅 호출 순서, startMonth 1~12.
2. 통합 하네스(실제 184 데이터): **184년 2월 시작 → 6월 진행 시 7월 수확 훅 발화 → 12월→185년 1월** 확인.
3. 실게임 `lovec project\src` 4초 부팅 — **크래시 없음**, 에러 로그 공백.
4. (수동 잔여) 실제 창에서 턴 버튼 클릭 시 월 증가·패널 표시 육안 확인 — 사용자 검증 항목.

### 회고(추가)
- 어제 만든 game_state(장수 규칙)에 턴 규칙을 같은 계층으로 얹어, main 은 버튼 클릭→위임만.
  훅/스텁으로 미구현 시스템의 호출 지점·순서를 먼저 확정(미완성 인터페이스 먼저 잡기)해
  나중에 세금·수확·AI를 콜백만 끼워 붙일 수 있게 됨.
- 다음(GDD 9·10장): onGrowth(세금·인구)·onHarvest(군량) 실제 계산, 내정/훈련에 행동완료 플래그 적용.

---

## 추가 작업 — 장수 확인 팝업 + 스탯/장비 표시 + 선물(금/장비) 시스템
GDD 7·8·15·17장. 선물은 "플레이어 세력 장수"에게만 → 군주/세력 선택이 전제라 최소 세력 선택 단계도 도입.

### 사전 결정 (사용자 확인)
선물이 ① 플레이어 세력 ② 군주 금 ③ 장비 데이터를 전제하나 셋 다 미존재(군주 선택 작업 중단)였음. 질문으로 합의:
- **플레이어 세력**: 시나리오 선택 후 "최소 세력(군주) 선택" 단계 추가.
- **금 선물**: 실동작 — 플레이어 군주에 시작 금 상수 부여, 차감/충성+5/부족 불가/턴 1회.
- **장비**: 스텁 — 상세에 "없음" 표시, 장비 선물 버튼 비활성(GDD 8장 데이터는 후속).

### 구현 내용
| 파일 | 계층 | 변경 |
| --- | --- | --- |
| `config.lua` | 상수 | `config.faction.startGold` + `config.gift`(goldGiftMax=10, loyaltyPerGold=5) + `config.popup`(레이아웃) |
| `game_state.lua` | 규칙 | 선물 규칙(`isGiftTarget`/`canGiftGold`/`applyGiftGold`) + 선물 플래그 리셋(resetActions) + buildOfficers 플래그/equip 초기화 + `EQUIP_IMPLEMENTED=false` |
| `main.lua` | 표현·입력 | 세력 선택 씬(`faction`) + 플레이어 흰 테두리(GDD 6장) + 군주 금 표시 + "장수 확인" 버튼 + 팝업 위임 + ESC 레이어 |
| `popup.lua` | 표현·입력(신규) | 장수 목록/상세/금 선물 서브팝업의 draw + 모달 클릭 처리(`Popup.draw`/`Popup.consumeClick`) |
| `tests/test_pure.lua` | 테스트 | 선물 규칙 17건 추가 |

### 흐름 (GDD 3·17장)
시나리오 선택 → **세력(군주) 선택** → 지도. 군주 = 세력 lord 장수(별도 타입 없이 `playerFactionId`로 표시).
지도 우측 패널: 연/월·세력·**군주 금** 표시 + "장수 확인"(선택 지역 있을 때 활성) + 턴 종료.
"장수 확인" → 목록 팝업 → 항목 클릭 → 상세(스탯/충성/병력/소속/위치/상태 + 장비"없음") → (플레이어 장수면) 금 선물 서브팝업.

### 선물 규칙 (game_state, 순수)
- `isGiftTarget`: 플레이어 세력 + active + 비수장(수장은 충성 '-' → 제외).
- `canGiftGold(gold, o, amount)`: 수장/이미선물/금액범위(1~10)/금부족 검사.
- `applyGiftGold(gold, o, amount)`: 충성 += amount×5 (maxLoyalty 클램프), 금 차감, 턴 플래그 set. (테이블=참조 변경, 숫자=반환)
- 턴 1회 제한: `giftedGoldThisTurn`/`giftedEquipThisTurn`를 `resetActions`(턴 시작)에서 행동완료와 함께 리셋.

### 모달 팝업 (popup.lua)
- 레이어(앞→뒤): 금 선물 서브팝업 > 상세 > 목록. `Popup.consumeClick`이 열린 레이어 기준으로 X/항목/−+/확정 처리.
- ESC는 안쪽부터 한 겹씩 닫기(서브→상세→목록→씬). 팝업 열림 중 지도 입력 차단(모달).

### 시도·실패 / 원인 / 수정
- **main.lua 880줄 → 800줄 규칙 초과(CLAUDE).** 팝업(렌더+클릭, ~250줄)을 `popup.lua`로 분리.
  - `state`만 주입(나머지 의존은 require). main 618줄 / popup 294줄로 정리.
  - 분리 시 `drawMap`이 뒤에 정의된 `drawPopups`를 호출하던 전방선언 문제도 모듈화로 자연 해소.
- **테스트 하네스에서 `require("main")` 시 멈춤.** 원인: main 은 love 콜백 모듈이라 require 부적합(에러 화면 대기 추정).
  - 수정: 하네스는 popup 로드 + 헤드리스 통합 테스트만, main 검증은 실게임 부팅으로.
- standalone Lua 부재 → 종전과 동일 lovec 하네스 사용.

### 검증
1. 순수 테스트 **94/94 통과**(선물 17건 추가): isGiftTarget(세력/수장/타세력/재야), canGiftGold(정상·수장·이미선물·0금·상한초과·금부족), applyGiftGold(차감·충성+5·플래그·상한클램프), resetActions 선물 플래그 리셋, buildOfficers 초기값.
2. **헤드리스 통합(실제 데이터+popup 글루)**: 성도 목록 클릭 → 상세(마속) → 금 선물 → 확정 3금 → **금 500→497, 충성 82→97(+15)**, 서브 닫힘 확인.
3. 실게임 `lovec project\src` 4초 부팅 — **크래시 없음**, 에러 로그 공백.
4. (수동 잔여) 실제 창에서 세력 선택·흰 테두리·팝업/ESC·금 선물 비활성 토글 육안 확인 — 사용자 검증 항목.

### 회고(추가)
- game_state(규칙)에 선물 함수를 순수로 두어 17건 자동 검증 + 헤드리스로 UI 글루까지 검증할 수 있었다.
  표현(popup)은 규칙 결과를 state 에 반영만 → 3계층 경계 유지.
- 800줄 규칙에 걸려 popup 을 분리하니 main 의 책임(씬/패널/입력 라우팅)이 더 또렷해졌다.
- 다음(GDD 8·15장): 장비 데이터 도입 → 장비 선물 실동작(보너스 이전), 금 선물의 군주 금을
  세수 수입(GDD 9장)과 연결(현재 startGold 상수는 placeholder).
