# CLAUDE.md — 삼국 패권

> **단일 진실 공급원 규칙 (반드시 준수)**
> 디자인 수치·규칙의 원본은 **GDD (`docs/GDD.md`)** 하나다.
> 이 문서는 "어떻게 짜는가"(아키텍처·원칙·스코프)만 다룬다. 수치를 여기 복붙하지 말고 **"GDD N장 참조"** 로 가리킬 것.
> GDD와 충돌하면 **항상 GDD를 따른다.**

## 프로젝트

- **삼국 패권** — 턴 기반 지역 점령 전략 게임. 시나리오·군주 선택 후 내정·징병·전쟁으로 천하 통일.
- 개인 프로젝트 / LÖVE2D 11.5 · Lua / PC.
- 전체 명세: `docs/GDD.md`

## 핵심 아키텍처 결정 (이것만은 반드시 인지)

1. **3계층 분리.** 데이터(`game_data.lua`) / 규칙(`game_state.lua`) / 표현·입력(`main.lua`). 계층을 섞지 말 것. → 모듈 역할: GDD 20장.
2. **데이터 주도.** 시나리오·지역·장수·건물·장비는 전부 `game_data.lua` 테이블. 콘텐츠 추가 = 데이터만 추가, 규칙 코드 수정 X.
3. **규칙 로직은 LÖVE 비의존 지향.** `game_state.lua`는 `love.*` 호출 없이 순수 Lua로 → `project/tests/`에서 단위 테스트 가능. 렌더/입력만 `main.lua`.
4. **update와 draw 책임 분리.** `draw`에서 상태를 변경하지 않는다(읽기만).
5. **암묵적 전역 금지.** 모든 모듈은 `local`로 선언하고 테이블을 `return`한다.
6. **유틸 분리.** 지역 조회/인접/클릭은 `region.lua`, 버튼 렌더/클릭은 `ui.lua`, 카메라 좌표/줌은 `camera.lua`.
7. **수치는 매직넘버 금지.** 비용·한도·확률은 `game_data.lua`/`config.lua` 상수로. 원본은 GDD.

## 폴더 컨벤션

```
project/src/      실행 코드 (모듈별 .lua)
project/src/assets/  폰트 등 리소스
project/tests/    순수 Lua 단위 테스트/하네스
docs/             설계 문서 (GDD.md)
worklogs/         작업 로그·회고
README.md         설치/실행/검증
run.bat           실행 진입 (lovec project/src)
```

파일 800줄 넘으면 책임 단위로 분리. (예외: 데이터 테이블 파일)

## 금지 패턴 (하지 말 것)

- ❌ 암묵적 전역 변수 (모듈 간 상태 누수)
- ❌ `draw`에서 게임 상태 변경
- ❌ 규칙 로직에 `love.*` 의존 섞기 (테스트 불가해짐)
- ❌ 수치 매직넘버 산재 — 상수/데이터로
- ❌ GDD에 없는 콘텐츠(지역/장수/건물/장비/시스템) 임의 추가 — 스코프 락
- ❌ 거대 단일 파일 방치 (한 모듈 한 책임)
- ❌ 과설계: 1회용 추상화, 불필요한 메타테이블 상속, 5줄 래퍼 모듈

## 스코프 락

구현 범위 = GDD에 정의된 것 전부, 그 이상은 추가 금지.
GDD에 없는 아이디어는 중간 투입 금지 → 메모해두고 나중에.
로드맵: GDD 21장(지도/카메라 → 시나리오/선택 → 내정/세금/턴 → 훈련/징병/이동 → 전투/포획/장수관리 → 그리드 전투 → 다중장수/세이브·로드 → 테스트/문서).

## 완벽주의 금지

기능별 80% 완성도로 끝내고 다음으로. 비주얼 폴리싱은 후순위.
미완성도 인터페이스(모듈 경계·함수 시그니처)는 먼저 잡아두기.

## 자가 점검표 (작업 후 확인)

- [ ] 데이터/규칙/표현 3계층이 섞이지 않았는가?
- [ ] `update`/`draw` 책임이 분리됐는가? (draw는 읽기만)
- [ ] 암묵적 전역이 없는가? (모든 모듈 local + return)
- [ ] 새 지역/장수/장비 추가가 `game_data.lua`만으로 되는가?
- [ ] 규칙 로직이 `love.*` 비의존이라 테스트 가능한가?
- [ ] 800줄 넘는 파일이 없는가? (데이터 파일 예외)
- [ ] 수치가 GDD와 일치하는가? (매직넘버 없음)
- [ ] 주석만 읽고도 모듈/함수 흐름이 이해되는가? (작성자가 직접 설명 가능)

## 주석/문서화 규칙 (학습용 — 자세히)

목적: 작성자가 AI가 짠 코드를 **직접 읽고 설명**할 수 있어야 한다. 주석은 한국어로 자세히 단다.

- **모듈 상단**: 이 모듈의 책임 2~3줄 + 다른 모듈과의 관계.
- **함수마다**: 무엇을 하는지 / 매개변수 의미 / 반환값 / 부작용(상태 변경 여부). 짧아도 반드시.
- **게임 규칙·공식**: 계산식 위에 "왜 이렇게 계산하는지" + 관련 GDD 장 표기.
  예: `-- 세금 = f(상업, 토지가치, 민충성, 정치) — GDD 9장`
- **매직넘버/상수**: 의미와 단위를 주석으로. 가능하면 이름붙인 상수로.
- **LÖVE2D API**: 처음 쓰는 `love.*` 함수엔 한 줄 설명(무슨 일 하는지).
- **비자명한 분기/엣지케이스**: 왜 이 조건이 필요한지.
  예: 출발지 장수 0명이면 병력 버리는 이유.
- **자명한 코드**(`i = i + 1`)엔 달지 말 것 — WHAT 말고 **WHY** 위주.
  단 학습 목적이라 **게임 규칙·LÖVE API·좌표 변환·전투/세금 공식**은 넉넉히 설명.
- **미완성/임시**: `-- TODO:` 로 명시.

스타일:
- 줄 주석 `--`, 블록 `--[[ ]]`. 함수 문서는 `---` 3줄 주석으로 함수 위에.

```lua
--- 지역 세금을 계산한다 (GDD 9장).
-- 상업·토지가치·민충성에 수행 군주의 정치 능력을 곱해 가산한다.
-- @param region table  대상 지역
-- @param lord   table  수행 군주(정치 능력 반영)
-- @return number 이번 턴 징수 금액
local function calcTax(region, lord)
  -- 기본 세수 = (상업 + 토지가치) * 민충성 비율
  ...
end
```

각 빌드 세션에서 **새로 짠 코드엔 위 규칙대로 주석을 함께** 작성한다.

## 참고 문서

- `docs/GDD.md` — **디자인 단일 진실 공급원** (수치·규칙 원본)
- `README.md` — 설치/실행/재현 절차
- `worklogs/` — 일자별 작업 로그·회고

## 검증

LÖVE2D 실행:
```bat
love-11.5-win64\lovec.exe project\src
```
규칙 단위 테스트: `project/tests/` 의 순수 Lua 테스트를 `lua` 로 실행.

---

**작업 흐름**: 수치/규칙 → `docs/GDD.md`. 아키텍처 의문 → 이 문서의 결정·금지·점검표. 코드 수정 시 변경 파일과 검증 방법을 함께 설명.
